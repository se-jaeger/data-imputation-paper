name: data-imputation-paper
channels:
  - defaults
  - conda-forge
dependencies:
  - python>=3.8
  - pip
  - numpy
  - scipy
  - pandas
  - ipython
  - matplotlib
  - seaborn
  - pip:
     # add here only pip-packages that are not available in conda/conda-forge! E.g.:
  # for development only (could also be kept in a separate environment file)
  - jupyterlab
  - pytest
  - pytest-cov
  - tox
  - pre_commit
  - nbdime
  - nbstripout
  - sphinx
  - recommonmark
